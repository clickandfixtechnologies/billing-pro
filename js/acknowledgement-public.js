"use strict";
(()=>{
 const endpoint="https://efmducfzefuoisjkuerv.supabase.co/functions/v1/acknowledgement",key="sb_publishable_rqefBAJyooFc7kCF795YQA_kUb3pngs",token=new URLSearchParams(location.search).get("token"),root=document.getElementById("ack");let ack,session="",locationReady=false,serverIp="";
 const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;","\"":"&quot;"}[c])),date=v=>{const d=new Date(v);return Number.isNaN(d)?String(v||"—"):d.toLocaleDateString("en-GB")},time=v=>{const d=new Date(v);return Number.isNaN(d)?"—":d.toLocaleTimeString("en-GB")},money=v=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR"}).format(Number(v||0));
 const style=`<style>*{box-sizing:border-box}body{background:#edf3f7;color:#17212b;font:15px/1.5 Arial,sans-serif;margin:0;padding:18px}.ack-page{margin:auto;max-width:760px}.ack-card{background:#fff;border:1px solid #ccd8e1;border-radius:10px;box-shadow:0 5px 18px #16344b15;margin:12px 0;padding:20px}.ack-brand{color:#175ea8;font-size:13px;font-weight:700;letter-spacing:1.4px}.ack-card h1{font-size:23px;margin:5px 0}.ack-card h2{font-size:17px;margin:0 0 10px}.ack-summary{display:grid;grid-template-columns:1fr 1fr;gap:0;margin-top:14px}.ack-summary div{border:1px solid #d6e0e7;margin:-1px 0 0 -1px;padding:10px}.muted{color:#5d6c77}.hidden{display:none}.notice{background:#eef7ff;border-left:4px solid #175ea8;padding:12px}.success{background:#e9f8ed;border-left:4px solid #258344;padding:12px}.error{background:#fff0f0;border-left:4px solid #b73636;padding:12px}input,textarea{border:1px solid #aebdc8;border-radius:5px;font:inherit;padding:10px;width:100%}textarea{min-height:78px;resize:vertical}label{display:block;font-weight:700;margin:13px 0 5px}button{background:#175ea8;border:0;border-radius:5px;color:#fff;cursor:pointer;font:inherit;font-weight:700;margin:12px 7px 0 0;padding:10px 14px}button.secondary{background:#536574}.signature{background:#fff;border:1px dashed #5f7282;border-radius:5px;display:block;touch-action:none;width:100%}.automatic{font-size:13px}.automatic p{margin:4px 0}.ack-pdf{background:#fff;border:1px solid #334155;color:#17212b;font:10px/1.35 Arial,sans-serif;margin:0 auto;max-width:190mm;padding:7mm}.ack-pdf header{border-bottom:2px solid #175ea8;display:flex;justify-content:space-between;margin:-7mm -7mm 4mm;padding:5mm 7mm}.ack-pdf h1,.ack-pdf h2,.ack-pdf p{margin:0}.ack-pdf h1{font-size:16px}.ack-pdf h2{font-size:14px;text-align:right}.ack-grid{display:grid;grid-template-columns:1fr 1fr;border:1px solid #94a3b8}.ack-grid>div{border-bottom:1px solid #cbd5e1;padding:3mm}.ack-grid>div:nth-child(odd){border-right:1px solid #cbd5e1}.ack-statement,.ack-condition,.ack-lines{border:1px solid #94a3b8;border-top:0;padding:3mm}.ack-lines p{border-bottom:1px solid #64748b;height:8mm;margin:5mm 0}.ack-disclaimer{font-size:8px;margin:2mm 0 0;text-align:center}@media(max-width:560px){body{padding:10px}.ack-summary{grid-template-columns:1fr}.ack-card{padding:16px}}@media print{@page{size:A4 portrait;margin:10mm}body{background:#fff;padding:0}.ack-card,.ack-page>h1{display:none}.ack-pdf{display:block!important;max-width:none}}</style>`;
 const call=async(action,data={})=>{const r=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json",apikey:key},body:JSON.stringify({action,token,...data})}),b=await r.json().catch(()=>({}));if(!r.ok||b.error)throw Error(b.error||"Acknowledgement service failed.");return b};
 const summary=a=>`<div class="ack-summary"><div><b>Acknowledgement No.</b><br>${esc(a.acknowledgementNo)}</div><div><b>Invoice No.</b><br>${esc(a.invoiceNo)}</div><div><b>Invoice Date</b><br>${date(a.invoiceDate)}</div><div><b>Delivery Date</b><br>${date(a.deliveryDate)}</div><div><b>Customer</b><br>${esc(a.customerName)}<br>${esc(a.customerAddress)}</div><div><b>Invoice Total</b><br>${money(a.invoiceTotal)}</div></div>`;
 const automatic=a=>`<section class="ack-card automatic"><h2>Automatic Information</h2><p>Verified Date: ${date(a.verifiedAt)}</p><p>Verified Time: ${time(a.verifiedAt)}</p><p>IP Address: ${esc(a.signedIp||serverIp||"Captured server-side")}</p><p>Delivery Location: ${esc(a.deliveryLocation||"Latitude/longitude captured")}</p><p>Latitude: ${esc(a.latitude)}</p><p>Longitude: ${esc(a.longitude)}</p><p>Verification: ${esc(a.verificationMethod||"Email OTP")}</p></section>`;
 const pdf=a=>`<section class="ack-pdf"><header><div><h1>CLICK &amp; FIX</h1><p>Acknowledgement / Delivery Receipt</p></div><div><h2>ACKNOWLEDGEMENT</h2><p><b>No:</b> ${esc(a.acknowledgementNo)}<br><b>Status:</b> SIGNED</p></div></header>${summary(a)}<section class="ack-statement">This acknowledgement confirms receipt of the above goods/services in satisfactory condition.</section><section class="ack-condition"><b>Delivery Condition</b><br>Goods/Service received in good condition &nbsp; ☑ Yes<br>Any issue/remark: ${esc(a.issueRemark||"")}</section><section class="ack-lines"><p>Receiver Name: ${esc(a.receiverName)}</p><p>Received By: ${esc(a.receiverName)}</p><p>Digital Signature: <img src="${a.signatureData}" style="height:18mm;max-width:65mm;vertical-align:middle"></p><p>Signed Date/Time: ${date(a.signedAt)} ${time(a.signedAt)}</p></section><section class="ack-condition"><b>Automatic Information</b><br>Verified: ${date(a.verifiedAt)} ${time(a.verifiedAt)} · Email OTP<br>IP Address: ${esc(a.signedIp||serverIp||"Captured server-side")}<br>Delivery Location: ${esc(a.deliveryLocation||"")}<br>Latitude: ${esc(a.latitude)} · Longitude: ${esc(a.longitude)}<br>Signature Method: Digital</section></section><p class="ack-disclaimer">This is system generated acknowledgement does not require signature</p>`;
 const show=html=>root.innerHTML=`${style}<main class="ack-page">${html}</main>`;
 const reverse=async(lat,lon)=>{try{const r=await fetch(`https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${encodeURIComponent(lat)}&lon=${encodeURIComponent(lon)}`,{headers:{Accept:"application/json"}}),b=await r.json();return String(b.display_name||"")}catch{return ""}};
 const captureLocation=()=>new Promise(resolve=>{if(!navigator.geolocation)return resolve(false);navigator.geolocation.getCurrentPosition(async position=>{const {latitude,longitude}=position.coords,address=await reverse(latitude,longitude);try{const result=await call("capture-location",{latitude,longitude,locationAddress:address});ack={...ack,latitude,longitude,deliveryLocation:result.deliveryLocation||address||`${latitude}, ${longitude}`};locationReady=true;document.getElementById("locationMessage").innerHTML=`<div class="success">Delivery Location: ${esc(ack.deliveryLocation)}<br>Latitude: ${latitude}<br>Longitude: ${longitude}</div>`;document.getElementById("identityStep").classList.remove("hidden");resolve(true)}catch(error){document.getElementById("locationMessage").innerHTML=`<div class="error">${esc(error.message)}</div>`;resolve(false)}},()=>{document.getElementById("locationMessage").innerHTML="<div class=\"error\">Location permission is required to complete this acknowledgement. Please allow location access and reload.</div>";resolve(false)},{enableHighAccuracy:true,timeout:15000,maximumAge:0})});
 const verifyStep=()=>{show(`<section class="ack-card"><div class="ack-brand">CLICK &amp; FIX</div><h1>ACKNOWLEDGEMENT / DELIVERY RECEIPT</h1>${summary(ack)}<p class="notice">This acknowledgement confirms receipt of the above goods/services in satisfactory condition.</p></section><section class="ack-card"><h2>Delivery Location Verification</h2><p>Delivery location verification is required to complete this acknowledgement.</p><div id="locationMessage">Requesting location permission…</div></section><section id="identityStep" class="ack-card hidden"><h2>VERIFY YOUR IDENTITY</h2><label>Phone Number<input id="phone" inputmode="tel" autocomplete="tel"></label><button id="requestOtp">Verify</button><div id="otpStep" class="hidden"><label>Enter OTP<input id="otp" inputmode="numeric" maxlength="6" autocomplete="one-time-code"></label><button id="verifyOtp">Verify OTP</button></div></section>`);captureLocation();document.getElementById("requestOtp").onclick=async()=>{if(!locationReady)return;try{await call("request-otp",{phone:document.getElementById("phone").value});document.getElementById("otpStep").classList.remove("hidden")}catch(e){alert(e.message)}};document.getElementById("verifyOtp").onclick=async()=>{try{const result=await call("verify-otp",{otp:document.getElementById("otp").value});session=result.verificationToken;serverIp=result.serverIp||"";ack.verifiedAt=result.verifiedAt;ack.verificationMethod="Email OTP";signStep()}catch(e){alert(e.message)}}};
 const cropAndEncode=canvas=>{const ctx=canvas.getContext("2d"),data=ctx.getImageData(0,0,canvas.width,canvas.height).data;let left=canvas.width,right=-1,top=canvas.height,bottom=-1;for(let y=0;y<canvas.height;y++)for(let x=0;x<canvas.width;x++){if(data[(y*canvas.width+x)*4+3]>20){left=Math.min(left,x);right=Math.max(right,x);top=Math.min(top,y);bottom=Math.max(bottom,y)}}if(right<left)throw Error("Please provide your signature.");const pad=12,w=right-left+1+pad*2,h=bottom-top+1+pad*2,max=700,scale=Math.min(1,max/Math.max(w,h)),out=document.createElement("canvas");out.width=Math.max(1,Math.round(w*scale));out.height=Math.max(1,Math.round(h*scale));out.getContext("2d").drawImage(canvas,left-pad,top-pad,w,h,0,0,out.width,out.height);return out.toDataURL("image/webp",.82)};
 const signStep=()=>{show(`<section class="ack-card"><div class="ack-brand">CLICK &amp; FIX</div><h1>Delivery Acknowledgement</h1>${summary(ack)}</section>${automatic(ack)}<section class="ack-card"><h2>Delivery Condition</h2><label><input id="condition" type="checkbox" style="width:auto"> Goods/Service received in good condition — Yes</label><label>Any issue/remark?<textarea id="remark"></textarea></label><label>Receiver Name<input id="receiver" required></label><label>Received By<input value="${esc(ack.customerName)}" readonly></label><label><input id="confirm" type="checkbox" style="width:auto"> I confirm that I have received the above goods/services.</label><label>Digital Signature</label><canvas id="signature" class="signature" width="900" height="240"></canvas><button id="clear" class="secondary">Clear</button><button id="submit">Submit Acknowledgement</button></section>`);const canvas=document.getElementById("signature"),ctx=canvas.getContext("2d");ctx.lineWidth=3;ctx.lineCap="round";let drawing=false,started=false;const point=e=>{const r=canvas.getBoundingClientRect();return{x:(e.clientX-r.left)*canvas.width/r.width,y:(e.clientY-r.top)*canvas.height/r.height}};canvas.addEventListener("pointerdown",async e=>{drawing=true;canvas.setPointerCapture(e.pointerId);const p=point(e);ctx.beginPath();ctx.moveTo(p.x,p.y);if(!started){started=true;call("signature-started",{verificationToken:session}).catch(()=>{})}});canvas.addEventListener("pointermove",e=>{if(!drawing)return;const p=point(e);ctx.lineTo(p.x,p.y);ctx.stroke()});canvas.addEventListener("pointerup",()=>drawing=false);document.getElementById("clear").onclick=()=>ctx.clearRect(0,0,canvas.width,canvas.height);document.getElementById("submit").onclick=async()=>{if(!document.getElementById("condition").checked||!document.getElementById("confirm").checked)return alert("Both confirmation checkboxes are required.");if(!document.getElementById("receiver").value.trim())return alert("Receiver name is required.");try{const signatureData=cropAndEncode(canvas);await call("submit-signature",{verificationToken:session,receiverName:document.getElementById("receiver").value.trim(),issueRemark:document.getElementById("remark").value,conditionConfirmed:true,signatureData});const signed=(await call("public-read")).acknowledgement;signedPage(signed)}catch(e){alert(e.message)}}};
 const download=async()=>{try{const result=await call("download-signed"),a=result.acknowledgement;const host=document.createElement("div");host.style.cssText="left:-100000px;position:fixed;top:0;width:190mm";host.innerHTML=`${style}${pdf(a)}`;document.body.append(host);try{if(!window.html2canvas||!window.jspdf?.jsPDF)throw Error("PDF generator is unavailable.");const node=host.querySelector(".ack-pdf"),canvas=await html2canvas(node,{scale:2,useCORS:true,backgroundColor:"#fff"}),doc=new window.jspdf.jsPDF({orientation:"p",unit:"mm",format:"a4"}),width=190,height=canvas.height*width/canvas.width;doc.addImage(canvas.toDataURL("image/jpeg",.95),"JPEG",10,10,width,height);doc.save(`Acknowledgement-${a.acknowledgementNo}.pdf`)}finally{host.remove()}}catch(e){alert(e.message)}};
 const signedPage=a=>{show(`<section class="ack-card"><div class="success"><h1>SIGNED</h1><p>This acknowledgement has already been signed.</p><button id="download">Download Acknowledgement</button></div></section>${pdf(a)}`);document.getElementById("download").onclick=download};
 (async()=>{if(!token)return show("<section class=\"ack-card error\">Invalid acknowledgement link.</section>");try{ack=(await call("public-read")).acknowledgement;if(ack.status==="SIGNED")return signedPage(ack);verifyStep()}catch(e){show(`<section class="ack-card error">${esc(e.message)}</section>`)}})();
})();
